# Core libraries for YouTube Data API and Gemini API
google-api-python-client
google-auth-httplib2
google-auth-oauthlib
google-generativeai
python-dotenv

# Note: 
# - Ensure ffmpeg is installed separately on your system if not already present.
#   (Needed by pydub, though pydub is removed, ffmpeg might be useful generally)
# - yt-dlp is no longer required by the core metadata/transcription/classification pipeline.
# - pydub is no longer required by the core pipeline. 